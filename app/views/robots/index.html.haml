%h1 ProBot!
%p An adaptive assistant for any application
%p
  = link_to 'New Robot', new_robot_path
%table
  %tr
    %th Name
    %th Job
  -@robots.each do |robot|
    %tr
      %td= robot.name
      %td= robot.job
      %td= link_to 'Edit', edit_robot_path(robot)
      %td= link_to 'Delete', robot_path(robot),                |
           method: :delete, data: { confirm: 'Are you sure?' } |
