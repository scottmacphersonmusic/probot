.header_container
  %h1 ProBot
  %h5 an adaptive assistant for any application
%table.table.table-hover
  %tr
    %th Name
    %th Job
  -@robots.each do |robot|
    %tr
      %td= link_to robot.name, robot_path(robot)
      %td= robot.job
      %td.btn.btn-xs.btn-info= link_to 'Edit', edit_robot_path(robot)
      %td.btn.btn-xs.btn-danger= link_to 'Delete', robot_path(robot),                |
           method: :delete, data: { confirm: 'Are you sure?' } |
